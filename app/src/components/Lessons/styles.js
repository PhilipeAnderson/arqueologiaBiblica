import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        marginBottom: 10,
    },
    row: {
        flexDirection: 'row',
        marginBottom: 10
    },
    capa: {
        width: 130,
        height: 70,
        marginRight: 10
    }
})

export default styles