import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    header:{
        position: 'absolute',
        top: 0,
        zIndex: 9999
    },
    headerSafeAreaView:{
        
    }
});

export default styles;